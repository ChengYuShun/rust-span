[package]
name = "cys-span"
version = "0.1.0"
edition = "2021"

[features]
derive_macros = ["dep:cys-span-derive"]

[dependencies]
cys-span-derive = { path = "derive", optional = true }

[dev-dependencies]
trybuild = "1.0"
